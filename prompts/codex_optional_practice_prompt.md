# Codex Prompt — Optional Practice

Read `AGENTS.md`, `docs/dataset_setup.md`, `docs/notebook_style_guide.md`, `docs/git_workflow.md`, and `docs/optional_practice_instructions.md` before coding.

Work on branch `optional-practice`.

Implement and execute `notebooks/optional_practice.ipynb`.

Important requirements:

- Use the student's filled manual selections.
- Stop if required manual selections are empty.
- Keep important code inside the notebook.
- Explain every method.
- Include `How I solved this task` for every optional task.
- Do not duplicate expensive work already completed in Part A unless needed.
- Execute the notebook and keep outputs.
- Commit progress frequently.
- Push branch and open PR if possible.

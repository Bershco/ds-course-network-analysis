# Codex Prompt — Part A

Read `AGENTS.md`, `docs/dataset_setup.md`, `docs/notebook_style_guide.md`, `docs/git_workflow.md`, and `docs/part_a_instructions.md` before coding.

Work on branch `part-a`.

Implement and execute `notebooks/part_a.ipynb` for all Part A tasks, including A1-A7.

Important requirements:

- Use the student's filled manual selections in `docs/part_a_instructions.md`.
- Stop if any required manual selection is empty.
- Keep important code inside the notebook.
- Use helper functions only if reused across cells.
- Explain every method before and after code.
- Include `How I solved this task` for every task.
- Export graph files for Cytoscape and Gephi.
- Add screenshot placeholders where GUI screenshots are required.
- Never load the full chess graph into NetworkX.
- Execute the notebook and keep outputs.
- Commit progress frequently.
- Push branch and open PR if possible.

At the end, print:

1. What was implemented.
2. Which datasets/files were used.
3. Which notebook cells still need manual screenshots.
4. Where exported files are located.
5. Git branch/commit/PR status.
